# Changelog

## 未发布

### 新增：火山账号网页登录（免 AK/SK、无需实名认证）
- 添加账号时可在「AK/SK」与「网页登录（免密钥）」两种方式间二选一。网页登录走火山官方公开 OAuth（same-device loopback + PKCE）：点按钮在系统浏览器打开 **火山官方域名** 登录授权，App 全程不接触账号密码，授权后浏览器自动跳回 App。
- 适用范围：网页登录账号**仅支持 Agent Plan / Coding Plan**（未实名火山账号也可查这两个套餐，已用真实未实名账号实测 HTTP 200）；语音服务仍需 AK/SK，网页账号在设置里隐藏语音配置、面板也不出现语音卡。
- 凭证机制：授权后拿到 15 分钟临时 STS（含 `X-Security-Token`，纳入 V4 签名）只在内存缓存、不落盘；长效 `refresh_token` 按账号 UUID 存钥匙串（键 `rt_<id>`）。STS 到期前 90 秒自动用 refresh_token 续，同一账号并发刷新去重。
- **48 小时硬过期**：火山 refresh_token 自签发起 48 小时硬过期、刷新不轮换也不滑动续期。到期后账号标记为「需要重新授权」：面板账号名旁出橙色「重新授权」入口、设置详情顶部出提示条；点击走一遍同样的浏览器授权即可恢复（账号 UUID / 别名 / 开关不变，且必须授权给同一火山账号，绑错会被拒绝）。
- 类型标识：面板账号名旁的徽章始终显示「网页登录 · 剩 X 小时」（展开/折叠都显示；不足 1 小时显示分钟，过期变橙并提示需重新授权）；设置侧边栏网页账号显示 globe 图标。账号标题行**不再显示 Agent Plan 的套餐档位**（如 Medium）——档位只属于对应套餐卡，账号层不混入任何单套餐信息。
- 多账号与去重：网页账号与 AK/SK 账号一样按火山账号 ID 去重，同一火山账号无论何种登录方式都不能重复添加；两种账号可任意混用、各自独立管理。
- 完全兼容旧版本：新增字段（`authMethod` / `webTokenExpiresAt`，配置 + 钥匙串 refresh_token）全部可选，旧配置无这些键时一律按 AK/SK 解码，老账号/老配置零迁移、不受影响。
- 零命令行依赖：OAuth 本地回调用原生 Network.framework 起一次性 127.0.0.1 随机端口监听实现，未引入 arkcli / node 等任何捆绑工具。

### 修复：折叠账号后面板脱离菜单栏、上下露出透明缝隙
- 现象：面板内容变矮（折叠账号、尤其内容较少时）后，`MenuBarExtraWindow` 高度只增不减，SwiftUI 内容在变高的窗口里垂直居中，而不透明底色只铺满内容本身——于是面板上沿离开菜单栏、下沿悬空，上下露出透明窗口，后方网页文字会透过来；折叠越多历史峰值越高、缝越大（已通过打印窗口 frame 证实：内容 364pt 时窗口一度被撑到 707pt）。
- 修复：不透明背景视图在布局时把弹窗窗口回缩到内容真实高度，顶边保持锚定菜单栏不动，只回收系统多留的高度；窗口正常增长仍交给系统。

### 修复：网页登录重启后无法取数（refresh 响应解码失败）
- 根因：用 refresh_token 续 STS 时，服务端响应**不回传 `refresh_token` 字段**（与 48h 不轮换一致），而本地 `TokenResponse.refresh_token` 是非可选 `String`，导致整个响应 JSON 解码失败、网页账号冷启动后必然报错。首次登录能用是因为授权码换发的响应里带该字段。
- 修复：`refresh_token` 改为可选解码；首次登录仍强校验非空，续期路径复用上一次存的 refresh_token。新增回归测试覆盖「刷新响应缺该字段仍可解码并取出 STS」。

### 新增：火山 Coding Plan 套餐卡片
- 同一账号可同时订阅 Agent Plan 与 Coding Plan：设置「套餐」分组新增 **Coding Plan** 卡片，独立开关 + 「测试」按钮，侧边栏摘要显示 `Agent Plan · Coding Plan · 语音 ×N`。
- 数据来源：火山 OpenAPI `GetCodingPlanUsage`（`ark` / `cn-beijing`，AK/SK 签名，已用真实账号验证 HTTP 200）。官方只给各窗口**已用百分比**与重置时间（秒级），不给绝对额度，因此卡片照搬百分比口径（`已用 X%` + 进度条 + `剩 X%` + 重置倒计时），不反推请求次数。
- 窗口与 Agent Plan 对齐：session（官方口径即 5 小时滚动窗口）/ 每周 / 每月；session 未起算时官方返 `ResetTimestamp=-1`，不显示重置时间。未订阅返 `Status=Reclaimed` 且无 `QuotaUsage`，测试按钮提示「该账号未订阅 Coding Plan」。
- 展示逻辑与 Agent Plan 完全一致：同一套标准/紧凑样式、折叠摘要（`5h/周/月`）、逐行钉选菜单栏指标（图标 `c.circle`）、错误态与重排顺序（Agent Plan → Coding Plan → 语音）。
- 配置 schema 新增 `enableCodingPlan`（可选键，缺失即关），旧配置直接可读，无需迁移；刷新调度新增 `coding-plan` 源，设置页单一刷新间隔仍同时写入所有源。

### 统一：语音服务卡也可单独折叠
- 语音卡（ASR/TTS）与 Agent Plan / Coding Plan 折叠逻辑完全一致：点标题行折叠为一行带色摘要（`ASR 48.7%`）、chevron 同规则显隐且同列、折叠态不响应钉选（展开后点卡身才钉到菜单栏）；再点标题行展开恢复明细。
- 单服务折叠摘要统一走 `SummaryBuilder.metrics(for: Service)`，三种服务形态一个入口。

### 修复：折叠摘要分行 + 折叠箭头位置
- 账号折叠后摘要按服务分行：Agent Plan、Coding Plan、各语音服务各占一行（行首 a/c 圆圈或语音小图标），不再把两个套餐的窗口指标挤在同一行。
- 修复折叠 chevron 与人头图标贴在一起（间距仅约 8pt）、账号级与服务级箭头不列齐的问题：所有箭头统一对齐到面板最左 gutter（距左缘约 5pt），与人头图标间距约 9pt；已用辅助功能坐标实测确认。

### 修复：设置页改账户名时应用崩溃
- 修复设置页重绘时同步启动钥匙串助手并在主线程调用 `Process.waitUntilExit()`，导致 SwiftUI `AttributeGraph` 重入后 `SIGABRT` 的问题。
- 语音应用卡不再在 `body` 求值期间读取 AK/SK；只在用户点击「测试」时读取。所有钥匙串助手进程统一在串行后台队列等待，避免其他入口再次触发同类重入。

### 新增：账号与 Agent Plan 卡片可折叠
- **账号级折叠**：点账号标题行折叠/展开；折叠后收成一张摘要小卡——Agent Plan 三窗口（`5h/周/月`）和各语音应用（`ASR/TTS`）的剩余百分比带额度色列在一行，tier 徽章保留，有服务报错时显示橙色警告。
- **Agent Plan 卡折叠**：点 Plan 标题行可单独折叠为摘要行（`5h 82%  周 95%  月 76%`），语音卡本身小不折叠。
- **折叠按钮显隐**：展开态下 chevron 默认隐藏，鼠标悬停到账号行/Plan 标题行时淡入；折叠态常显，保证随时能发现可展开。
- 折叠状态按账号/服务持久化（UserDefaults），重启保持；账号删除时自动清理；新加账号默认展开。折叠态不显示钉选热区（要钉指标先展开），菜单栏已钉指标不受影响。
- 摘要数字与展开态同源、同字号、同颜色分级，只是浓缩排布；标准/紧凑两种样式下行为一致。

### 修复：每次启动/更新后钥匙串反复弹授权（6 次）

> 本次彻底定位并修复。根因与之前以为的不同：macOS 对「自签名/ad-hoc」签名的**图形化 app**，在访问钥匙串时按**二进制哈希（cdhash）**记忆授权，且 `kSecUseNoAuthenticationUI` 对 GUI 访问无效的弹窗无法靠它抑制；主程序每次改代码重编译 cdhash 都变，于是 3 账号 × AK/SK 共 6 个条目每次更新后都重新弹「始终允许」。固定自签证书也不解决——证书不受系统信任，授权仍退回到按 cdhash 记。
- 修复一（架构）：新增**冻结式钥匙串助手** `CredHelper`，把钥匙串读写从主程序移出，收敛到一个「签名一次、之后永不再编译」的固定小助手（`~/Library/Application Support/My Quota Bar/credhelper-v1`）。助手二进制恒定，对它点一次「始终允许」即永久有效；主程序之后随便重编译都不再直接碰钥匙串，因此不再弹窗。助手会校验父进程必须是同一证书签名、identifier 为 `local.my.quota-bar` 的本 app，其他进程调用一律拒绝（密钥经 stdin/stdout 传递，不进命令行参数）。
- 修复二（迁移）：构建脚本在首次安装助手后运行 `credhelper-v1 migrate-all`，一次性把 ad-hoc 时期的旧条目 ACL 重锚到本项目证书身份（只改访问控制、不读密钥数据），旧条目因此不需要再逐个弹授权。
- 保留：`build-app.sh` 用固定的本地自签名证书签名（`My Quota Bar Signing`，首次构建自动生成并导入钥匙串）。证书只用于身份隔离，不再依赖其受信任性。
- 验证：实测真实 App 在「启动 → 重编译（app cdhash 变化、助手 cdhash 不变）→ 再重编译」全程弹窗监控为空；终端直接调用助手被拒、同一证书签名的 app 调用正常返回密钥。
- 注意事项：若从旧版本直接升级后**首次启动**仍见一次授权弹窗，请点「始终允许」（这是对冻结助手的最后一次授权）；之后重建/重启都不再弹。

### 新增：Agent Plan 卡片紧凑样式（可切换，默认标准样式）
- 面板底部 footer 新增「紧凑样式 / 标准样式」文字按钮、右上角有图标按钮，「通用」设置页「外观」区也有开关；选择持久化，随时互切。
- 紧凑样式把进度条移入周期名与剩余百分比之间的空白，每个周期由 3 行变 2 行；字号、间距、数值、重置文案、钉选交互与标准样式完全一致，仅排布结构不同。
- 标准样式（原有布局）原样保留为默认。

### 交互重构：设置页改为「改了立即生效」
- 移除「草稿 + 保存按钮 + 未保存拦截」整套机制：改名、拨开关、增删语音应用、拖动排序都立即落盘并刷新面板，不再需要点「保存」，切换账号或关窗也不再弹「有尚未保存的修改」。
- 账号详情重排为「账号 / 套餐 / 语音」三个平级分组，一屏看全；整体更紧凑（语音应用卡压到 3 行，AppID 窄框与备注并排）。
- AK/SK 不再常驻可编辑输入框，收进「更换密钥…」弹窗：填新密钥后先「测试连接」，通过才允许保存。
- 顺带修掉草稿模型下的多个老问题：保存按钮文案与实际保存范围不符；服务「测试」测的是未保存草稿里的密钥；改密钥后 Agent Plan / 语音的旧绿色测试结果不清除；「保存并切换」失败后会误跳到目标账号。

### 信息结构调整
- 语音应用从「语音服务包裹卡内部的第三级」提为与 Agent Plan 平级的卡片：「套餐」放订阅套餐类（现 Agent Plan），「语音」是独立服务域，每个语音应用（AppID）直接平铺，「添加应用」按钮移到「语音」标题行右侧。
- **移除语音总开关**：语音是否取数/显示完全由各应用的 ASR/TTS 复选框决定，两个都不勾即不拉数、不显示（AppID/备注保留）。这与火山官方「一个 AppID 挂多个语音服务、资源包按服务独立计」的模型一致。

### 新增
- **语音应用内 ASR（语音识别）与 TTS（语音合成）可分别独立开关**。例如某账号 ASR 额度用尽（0%）时可只关 ASR、保留 TTS，面板不再显示 0% 的红色 ASR 卡。
- 「通用」页新增**开机自动启动**开关（`SMAppService`）。
- 「通用」页底部显示应用版本号。

### 账号区与密钥弹窗优化
- 账号信息区去重、收紧：去掉与左侧列表/顶部重复的平台与名称；压成两行——名称（可改）+ 更换密钥，平台 · 身份 · 账号 ID 一行（ID 可一键复制）。
- 套餐的 Agent Plan **测试按钮常显**（未开开关也能先验证），并与语音应用卡的测试按钮统一放到右侧。
- 添加 / 更换密钥弹窗的两个输入框加明确标签「Access Key ID（AK）」「Secret Access Key（SK）」，不再只靠占位符。
- 修正身份解析：正确识别主账号（Trn `...:root`）与子用户（`...:user/<名>`）；测试连接后写入身份标记，账号区显示「主账号 / 子用户 · 名称」徽章。
- 说明：火山 STS `GetCallerIdentity` 用 AK/SK 仅返回账号 ID 与 Trn，**官方不开放手机号 / 邮箱查询**，故不做手机号字段。

### 数据模型清理（不做历史兼容/迁移）
- 移除冗余的 `enableSpeech` 字段，语音启用态改为从 `speechApps` 派生（`SpeechApp.isActive` / `AccountConfig.hasActiveSpeech`）。
- `AccountConfig` / `SpeechApp` 改为编译器合成 Codable，删掉手写的防御式解码样板；历史遗留的多余字段（如 `enableSpeech`）解码时天然忽略。账号 UUID 与钥匙串凭证键不变，已配置的账号和密钥不丢。
- 「显示」Tab 更名为「通用」；Agent Plan / 语音两个刷新间隔合并为一个「刷新间隔」（内部仍按源调度）。

### 测试
- 单测 23 个：新增语音应用 `isActive` 派生（有效 AppID / 全关 / 只开一个）、账号语音启用态派生、历史多余字段被忽略三组测试；移除旧 schema 兼容/迁移与草稿快照测试。

## 1.1.0 — 2026-07-30

### 可靠性
- 钥匙串改为原地更新并检查 OSStatus；AK/SK 成组写入失败时回滚。
- 账号配置增加有效备份恢复和损坏写入锁，避免空配置覆盖原数据。
- 账号修改/删除使用运行时代数隔离，旧异步响应不能重新写回。
- STS 与 Agent Plan 保留 HTTP/官方错误信息。
- 语音 ASR/TTS 分项并发、独立容错，失败分项保留上次成功值。
- 同一类型的多个语音资源包全部保留，不再只取第一条。

### 架构与性能
- 新增 `PlatformAdapter`、`PlatformRegistry` 和火山平台适配器。
- 未知平台 ID 原样保存并显示为暂不支持，不会误写成火山引擎。
- 全 App 使用一个刷新调度 Timer，按源计算下次刷新；最多 4 个账号并发。
- 刷新失败使用指数退避；休眠、断网和恢复继续按过期状态调度。
- 删除已无调用的 ProcessRunner/arkcli/node 遗留代码。

### 交互
- 设置左栏按平台分组账号。
- 右侧明确分为“账号信息 / 服务管理”两个页面。
- Agent Plan 与语音服务同级；语音 AppID 作为服务内部实例。
- 语音服务增加独立开关，关闭时保留 AppID 配置。
- 切换账号或关闭设置窗口时保护未保存修改。
- 菜单面板按平台 → 账号 → 服务展示。

### 测试
- 单测增至 20 个，新增未知平台保留、平台适配注册、旧语音配置迁移、多资源包与官方错误信息测试。

## 1.0.0 — 2026-07-28

- 首个可用版本：多火山账号、Agent Plan、语音 ASR/TTS、钥匙串、菜单栏指标和独立刷新间隔。
