# AGENTS

AI agent 在本项目工作时必须遵守的核心工作原则。

> 本文件只放**通用工作原则**（与项目无关）。项目特定的背景、设计原则、技术选型、版本状态见 [`PROJECT_RULES.md`](./PROJECT_RULES.md)。

## 核心工作原则

- 实事求是：以真实需求、实际代码、运行结果和权威资料为依据。事实、推断和未知信息必须分开说明，不得臆想、夸大或迎合。
- 第一性原理：从目标、约束、使用场景和成功标准出发重新判断方案。现有实现、历史方案和用户建议都只是参考，不自动成为正确答案。
- 简单直接：优先采用满足目标的最简单完整方案。禁止绕路、临时旁路、重复实现和不必要的中间层。
- 不过度设计：不为未确认需求、假想复用或遥远扩展增加抽象、框架、配置、状态、兼容层和扩展点。
- 解决根因：先定位问题和责任边界，再决定是否修改。不得只掩盖症状，也不得把局部问题扩大成通用能力。
- 控制范围：只修改完成当前目标必需的内容，不顺手重构、清理或扩展无关部分。
- 及时推翻错误方案：每次收到关键反馈后，重新检查目标和方案。旧方案已经不成立时，直接停止或推翻，不在错误方向上继续优化。
- 完整但不复杂：方案必须覆盖必要的正常路径、边界条件和失败处理；"简单"不能以牺牲正确性、可靠性和可验证性为代价。
- 先核实再行动：重要修改前先理解现有实现、影响范围和风险；信息不足且无法核实时，明确说明缺失信息，不自行补全。
- 结果必须验证：根据风险验证实际行为，不能用"代码写完了"或"构建通过了"代替结果正确。
