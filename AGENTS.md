# My Quota Bar 项目规则

这是原生 macOS 菜单栏额度查看工具。开始工作前先读 `PROJECT_RULES.md`、`README.md`、`Package.swift` 以及任务相关的 `Sources/` 和 `Tests/`。

## 核心工作原则

- 实事求是：以真实需求、实际代码、运行结果和权威资料为依据。必须区分已验证事实、用户报告的现象、观点、推断、假设和未知信息；不得把用户或 Agent 的观点、推断、假设以及来源方主张写成已确认事实，也不得臆想、夸大、隐瞒不确定性或为了配合而迎合用户。
- 第一性原理：从目标、约束、使用场景、成功标准和可接受代价出发重新判断问题。现有实现、历史方案、行业惯例、用户建议和 Agent 之前的方案都只是参考，不自动成为正确答案。
- 需求先于方案：用户可能用“改大、改小、增加组件、使用某技术、按某结构实现”等方案语言表达需求。除非这是明确指令或已确认约束，否则先提炼要解决的问题、使用场景、预期结果和成功标准，再完成必要的设计并选择方案；不自动把用户提出的做法当成最终方案。
- 尊重明确指令：用户明确指定的交付结果、技术边界或实现方式，应作为指令或约束执行；如果它与已知事实、项目目标、安全要求或现有实现冲突，必须先指出冲突、依据和影响，不得静默照做或擅自改写。
- 前提必须成立：形成判断、调研结论或实施方案时，识别其依赖的关键前提，并区分哪些已经证实、哪些只是暂时假设。前提不成立会实质改变目标、范围、架构、数据、安全、成本或验收结果时，必须先通过项目证据或权威资料验证；无法验证时向用户确认。
- 目标优先：实施前确认真实问题、目标、成功标准和不做的范围；需求、前提或证据变化后，重新检查目标和方案是否仍然成立。验证不仅要确认代码按方案实现，还要确认原始问题已经解决、成功标准已经达到。
- 独立判断：发现用户、文档、代码、调研来源或方案存在错误、过时、证据不足或风险时，直接指出并说明依据，不默认附和，也不机械折中。
- 简单直接：优先采用满足当前目标的最简单完整方案。禁止绕路、临时旁路、重复实现和不必要的中间层；有更简单的方法时主动说明其适用边界、牺牲和风险。
- 不过度设计：不为未确认需求、假想复用或遥远扩展增加抽象、框架、配置、状态、兼容层和扩展点；只有真实复杂度、明确重复或稳定边界已经出现时才抽象。
- 解决根因：先确认现象、收集证据并定位问题和责任边界，再决定是否修改。不得把原因猜测当作根因，不得只掩盖症状，也不得把局部问题扩大成通用能力。
- 控制范围：只修改完成当前目标必需的内容，不顺手重构、清理或扩展无关部分；但必须检查必要的调用链、相似实现、配置、测试、文档和边界条件，避免只修用户点名的位置而遗漏真实影响范围。
- 及时推翻错误方案：收到关键反馈、发现新证据或验证失败后，重新检查事实、需求、前提和方案；旧方案不成立时直接停止或推翻，不在错误方向上继续优化。
- 先核实再行动：修改前阅读相关规则、文档、代码、配置、数据和当前状态，理解影响和风险。能通过文件、命令、运行结果或权威资料核实的先核实；关键信息仍缺失且会实质改变方案时，明确说明缺失信息、当前假设和风险，不自行补全。
- 完整但不复杂：方案应覆盖与实际风险相匹配的正常路径、边界条件、失败处理和恢复方式；简单不能以牺牲正确性、可靠性、安全性和可验证性为代价。
- 保护已有资产：不得擅自删除、覆盖、回滚或混入用户已有改动、数据和配置。涉及敏感信息、真实账号、生产环境、付费、部署、发布或不可逆操作时，先说明影响并取得确认。
- 结果必须验证：根据风险选择能够证明真实结果的验证方式，包括适用的测试、构建、类型或静态检查，以及必要的真实界面、接口、数据、日志、性能、设备和失败路径检查；不能用“代码写完了”“测试通过了”或“构建通过了”代替需求已经满足。
- 完成交付闭环：只有目标已实现、必要边界已处理、验证已通过、最终改动已检查并完成对应版本记录后，才可声明任务完成。结束时说明完成内容、实际验证结果、未验证项、风险和剩余工作，不把计划或部分实现表述为已经完成。
- 控制沟通成本：简单、明确且低风险的任务不强制生成需求文档、方案文档或信息分类表。只有歧义无法通过项目现状核实，并且会实质改变方案或结果时才阻塞询问；其他情况说明合理解释或暂时假设后继续。

## Git 与版本管理

- 项目必须使用 Git 管理。尚未初始化 Git 时，先确认项目根目录和忽略规则，避免把密钥、依赖、构建产物或大文件纳入版本库，再初始化仓库。
- 开始任务前检查当前分支、`git status` 和相关 diff，识别用户已有改动；无法确认归属时先询问，不得覆盖、提交或回退。
- 每个能够独立验证的功能、修复或规则调整完成并验证通过后，立即提交当前逻辑单元，形成清晰、可恢复的版本点；不要把多个无关任务混在一次提交中，也不要制造没有独立价值的碎片提交。
- 提交前检查 diff，只包含当前交付单元相关文件，不包含敏感信息、无关改动和不应入库的产物。提交信息应准确描述实际结果，不得声称完成尚未验证的内容。
- 未完成工作需要暂停、切换分支或执行高风险操作时，先保护现有改动；必要时经用户同意创建明确标记的本地 `WIP` 提交或保护分支。
- 未经用户明确授权，不得执行可能丢失工作的还原、清理、硬重置、强制覆盖或历史改写。回退前先确认范围并建立保护点；已提交错误优先通过新提交或 `git revert` 修复。是否推送远端或发布，遵循项目规则和用户明确要求。

## 产品和数据边界

- 保持“平台 -> 账号 -> 服务 -> 额度”的信息层级；各服务按官方原始结构展示，不擅自统一格式、压缩数值或改变单位。
- 新增平台、服务或字段前，确认官方数据源、字段语义、空值、错误行为和兼容策略，不根据名称猜测映射。
- AK/SK 等敏感凭证只能保存在 macOS 钥匙串；非敏感配置使用现有 UserDefaults JSON。不得硬编码、输出、提交或写入测试 fixture。
- 配置 schema 变更必须保持已有账号和服务配置可读取；迁移失败不得静默清空或覆盖用户配置。
- 网络请求仅访问已登记的官方接口。真实账户连接测试、额度查询和凭证验证前先说明影响，不在默认单元测试中依赖真实账号。

## 实现约束

- 保持 Swift Package Manager、原生 SwiftUI/AppKit、universal binary 和本地临时签名路线。
- 平台差异通过现有 `PlatformAdapter` 和服务 Provider 表达；不得把平台字段强行塞进统一业务模型。
- 状态管理、凭证存储、网络 Provider、数据模型和界面职责保持分离，只在出现真实复用时增加抽象。
- 只修改完成当前目标必需的内容，不顺手重写设置窗口、菜单栏刷新或数据展示逻辑。

## 验证和交付

- Swift 逻辑、配置兼容或 UI 改动至少运行 `swift test`；关键纯逻辑和 schema 演进需要回归测试。
- 需要验证可分发应用时运行 `./build-app.sh`，检查生成的 `outputs/My Quota Bar.app` 和 universal 构建结果。
- UI 改动除测试外，还要实际检查菜单栏、弹窗、设置窗口、连接状态和错误状态。停止或替换用户正在运行的应用前先说明影响。
- 不提交 `.build/`、`outputs/`、`pics/` 或包含账户信息的产物。
- 交付时列出改动、验证命令、实际结果和未验证部分。

## 规则维护

- 只保留长期有效、项目特定、能够执行和验证的规则；详细领域知识、阶段计划和临时状态放在专门文档中。
- 新规则应来自真实问题、确认需求或复盘经验。命令、路径、接口和数据口径必须经过项目文件或实际运行核实。
- 修改规则前说明目的和影响范围；不得静默删除陌生的领域规则，不确定时先核实或询问用户。
